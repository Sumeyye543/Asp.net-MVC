
@{
    ViewBag.Title = "Transfer1";
}

@using Banka.Models;

@model ViewModel


<div class="container-scroller">
    @Html.Partial("PartialView");


    <!-- partial -->
    <div class="main-panel">
            <div class="page-header">
                <h3 class="page-title"> Tranfer </h3>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="#"></a></li>
                        <li class="breadcrumb-item active" aria-current="page"></li>
                    </ol>
                </nav>
            </div>
            <div class="row">
                <div class="col-12 grid-margin stretch-card">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title"></h4>
                            @Session["mesaj"]
                            @{
                                Session["mesaj"] = "";
                            }
                            
                            <p class="card-description">  </p>
                            <form class="forms-sample" action="/Hesap/Gonder1/" method="post" role="form">

                                <div class="form-group">
                                    <label for="exampleInputPassword1">Gönderecek Hesap ID</label>



                                    <select class="form-control" name="id">
                                        @foreach (var item in Model.KullaniciHesaplari)
                                        {
                                            <option value="@item.hesap_id">@item.hesap_id</option>
                                        }
                                    </select>

                                </div>
                                <div class="form-group">
                                    <label for="exampleInputPassword1">Gönderilecek Hesap ID</label>



                                    <select class="form-control" name="id1">
                                        @foreach (var item in Model.HesaplarList)
                                        {
                                            <option value="@item.hesap_id">@item.hesap_id</option>
                                        }
                                    </select>
                                </div>
                                <div class="form-group">


                                    <label for="exampleInputName1">Gönderilecek Miktar</label>

                                    <input type="text" class="form-control" name="bakiye">


                                </div>


                                <button type="submit" class="btn btn-primary gonder">Aktar</button>

                            </form>

                        </div>
                    </div>
                </div>



            </div>
        </div>
    </div>
